# 📘 BloggerApp

A simple React application demonstrating **conditional rendering**, **component extraction**, **list rendering using `map()`**, and **usage of keys** in React.

---

## 📂 Project Structure

src/
├── App.js # Main application file
├── App.css # Styling for layout and spacing
├── index.js # React app entry point (uses createRoot)
├── BookList.js # Renders list of books using props + map()
├── BlogDetails.js # Static blog information component
├── CourseDetails.js # Static course information component
├── data.js # Book data array (sample)
public/
└── index.html


---

## 🚀 Features Demonstrated

- ✅ Conditional Rendering (`if`, ternary, logical &&)
- ✅ List rendering using `.map()`
- ✅ Using `key` props correctly in lists
- ✅ Extracted components (`BookList`, `CourseDetails`, etc.)
- ✅ Three-column layout using Flexbox
- ✅ Clean project structure

---

## 📦 Setup Instructions

1. **Clone the repo or download the project folder**
   ```bash
   git clone https://github.com/your-username/bloggerapp.git
   cd bloggerapp
2. Install dependencies
   npm install
3. Start the development server
   npm start
4. Visit in browser
   http://localhost:3000


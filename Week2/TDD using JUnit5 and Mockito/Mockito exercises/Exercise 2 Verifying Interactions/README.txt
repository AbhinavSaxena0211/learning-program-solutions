Exercise: Verify Method Interactions Using Mockito

Description:
Tests that a method (getData()) is called on a mock object. Uses Mockito.verify() to confirm interaction.

How to Run:

Make sure the pom.xml includes JUnit 5 and Mockito dependencies.

Open MyServiceTest.java and run it as a JUnit test.

